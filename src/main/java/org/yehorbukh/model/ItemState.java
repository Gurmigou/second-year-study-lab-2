package org.yehorbukh.model;

/**
 * Enum contains three different states of a record.
 * A record can be available, archived or deleted.
 */
public enum ItemState {
    AVAILABLE,
    ARCHIVED,
    DELETED
}
